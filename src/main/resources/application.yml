spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/spring_security
    username: root
    password: test


initialization:
  file:
    users: users.json
    roles: roles.json
    permissions: permissions.json
